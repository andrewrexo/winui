{
  "name": "@drewrube/winui",
  "version": "0.1.1",
  "description": "Windows-themed UI components for Svelte and React",
  "scripts": {
    "build": "pnpm -r run build",
    "check": "pnpm -r run check"
  }
}
